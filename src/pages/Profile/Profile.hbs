<main class="profilePage">
    {{>PrevLink src=icons.button}}
    <div class="profilePage__info">
        <div class="profilePage__changeAvatar">
            {{>ChangeAvatar title="Иван" avatarIcon=icons.avatar showModal=modal}}
        </div>
        <div class="profilePage__infoFields">
            {{>InfoField label="Почта" value="pochta@yandex.ru" }}
            {{>InfoField label="Логин" value="ivanivanov" }}
            {{>InfoField label="Имя" value="Иван" }}
            {{>InfoField label="Фамилия" value="Иванов" }}
            {{>InfoField label="Имя в чате" value="Иван" }}
            {{>InfoField label="Телефон" value="+7 (909) 967 30 30" type="last" }}
        </div>
        <div class="profilePage__links">
            <div>
                {{>Link content="Изменить данные" }}
            </div>
            <div>
                {{>Link content="Изменить пароль" }}
            </div>
            <div>
                {{>Link content="Выйти" color="red" }}
            </div>
        </div>
    </div>
</main>
